# Desktop Service

A Windows Service.

## How to use

1. Install services with command `install-service`, it will run `desktop-service` program and start the web service.
2. Program `desktop-service` requests: version, start, stop, info, set_dns, unset_dns.
3. Uninstall services with command `uninstall-service`.
